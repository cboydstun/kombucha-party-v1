# Stripe secret key — used only by the serverless checkout function (api/).
# Get it from https://dashboard.stripe.com/apikeys (use a test key, sk_test_…,
# while developing). Set the real value as an environment variable in the
# Vercel project settings; never commit the actual key.
STRIPE_SECRET_KEY=sk_test_xxx
